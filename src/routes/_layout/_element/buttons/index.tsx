import ButtonShowCase from '#/features/element/buttons'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_layout/_element/buttons/')({
  component: ButtonShowCase,
})

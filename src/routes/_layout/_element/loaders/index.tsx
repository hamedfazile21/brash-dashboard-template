import LoaderShowCase from '#/features/element/loaders'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_layout/_element/loaders/')({
  component: LoaderShowCase,
})

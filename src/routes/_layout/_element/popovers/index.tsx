import PopoverShowCase from '#/features/element/popovers'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_layout/_element/popovers/')({
  component: PopoverShowCase,
})
